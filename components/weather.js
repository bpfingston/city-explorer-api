const axios = require("axios");

const { response } = require("express");

function handleGetAbout(req, res){
    response.send('Hi from about');
}

function handleGetHome(request)

class Forecast{
    constructor (date, description){
    this.date = date;
    this.description = description;
    };
}